{
  // Solution file: one program per side (host half vs browser half), the rule
  // the upstream plugin packages follow — the host `sessions: SessionStore`
  // merge and the browser `sessions: ISessions` merge cannot share one
  // ts.Program (TS2717).
  "files": [],
  "references": [
    { "path": "./tsconfig.host.json" },
    { "path": "./tsconfig.client.json" }
  ]
}
